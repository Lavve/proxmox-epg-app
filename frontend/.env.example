# Optional API base URL. Leave empty to use same-origin /api (Vite proxy in dev, Nginx in production).
# VITE_API_BASE_URL=
# Optional backend target for the Vite dev/preview proxy (defaults to http://127.0.0.1:3001)
# VITE_DEV_BACKEND_URL=http://127.0.0.1:3001
